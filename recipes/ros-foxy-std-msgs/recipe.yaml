package:
  name: ros-foxy-std-msgs
  version: 2.0.3
source:
  git_url: https://github.com/ros2-gbp/common_interfaces-release.git
  git_rev: release/foxy/std_msgs/2.0.3-1
  folder: ros-foxy-std-msgs/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 0
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
  host:
    - python
    - ros-foxy-ament-cmake
    - ros-foxy-ament-lint-common
    - ros-foxy-builtin-interfaces
    - ros-foxy-rosidl-default-generators
  run:
    - python
    - ros-foxy-builtin-interfaces
    - ros-foxy-rosidl-default-runtime
