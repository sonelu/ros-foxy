package:
  name: ros-foxy-geometry-msgs
  version: 2.0.3
source:
  git_url: https://github.com/ros2-gbp/common_interfaces-release.git
  git_rev: release/foxy/geometry_msgs/2.0.3-1
  folder: ros-foxy-geometry-msgs/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 0
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
  host:
    - python
    - ros-foxy-ament-cmake
    - ros-foxy-ament-lint-common
    - ros-foxy-rosidl-default-generators
    - ros-foxy-std-msgs
  run:
    - python
    - ros-foxy-rosidl-default-runtime
    - ros-foxy-std-msgs
