package:
  name: ros-foxy-tf2-py
  version: 0.13.9
source:
  git_url: https://github.com/ros2-gbp/geometry2-release.git
  git_rev: release/foxy/tf2_py/0.13.9-1
  folder: ros-foxy-tf2-py/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 0
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
  host:
    - python
    - ros-foxy-ament-cmake
    - ros-foxy-ament-cmake-pytest
    - ros-foxy-ament-lint-auto
    - ros-foxy-ament-lint-common
    - ros-foxy-geometry-msgs
    - ros-foxy-python-cmake-module
    - ros-foxy-tf2
  run:
    - python
    - ros-foxy-builtin-interfaces
    - ros-foxy-geometry-msgs
    - ros-foxy-rclpy
    - ros-foxy-rpyutils
    - ros-foxy-tf2
